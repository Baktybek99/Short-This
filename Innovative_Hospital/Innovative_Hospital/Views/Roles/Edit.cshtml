@model ChangeRoleViewModel

<h3>Смена ролей для пользователя @Model.UserEmail</h3>

<form asp-controller="Roles" asp-action="Edit">
    <input asp-for="UserId" type="hidden" />
    <div class="form-group">
        @foreach (var role in Model.AllRoles)
        {
            <input type="checkbox" name="roles" value="@role.Name" @(Model.UserRoles.Contains(role.Name) ? "checked=\" checked\"" : "") />@role.Name <br />
        }
    </div>
        
    <input type="submit" />
</form>